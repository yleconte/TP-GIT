# eclipse
# enleve tout les fichiers/dossiers relatif a un projet eclipse qui sont useless pour les autres (config de l'ide/dossier de build)
.metadata
bin/
tmp/
*.tmp
*.bak
*.swp
*~.nib
local.properties
.settings/
.loadpath
.recommenders

# idea
# enleve tout les fichiers/dossiers relatif a un projet idea qui sont useless pour les autres (config de l'ide/dossier de build)
out
*.ipr
*.iws
*.iml
.idea

# gradle
# enleve les dossiers de build et de cache de gradle
build
.gradle

# Maven
# enleve les dossiers de build et de cache de maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn
